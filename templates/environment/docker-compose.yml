name: primarie-env-${ENV_KEY}

services:
  env-nginx:
    image: nginx:1.27-alpine
    depends_on:
      - admin
      - api
      - web
    ports:
      - "${ENV_PORT}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    environment:
      ENV_KEY: ${ENV_KEY}
      ROOT_DOMAIN: ${ROOT_DOMAIN}

  admin:
    image: nginx:1.27-alpine
    volumes:
      - ./mock/admin.html:/usr/share/nginx/html/index.html:ro
      - ./repo:/workspace/repo:ro

  api:
    image: nginx:1.27-alpine
    volumes:
      - ./mock/api.json:/usr/share/nginx/html/index.html:ro
      - ./repo:/workspace/repo:ro

  web:
    image: nginx:1.27-alpine
    volumes:
      - ./mock/web.html:/usr/share/nginx/html/index.html:ro
      - ./repo:/workspace/repo:ro

  mongo:
    image: mongo:7
    command: ["mongod", "--quiet"]
    volumes:
      - mongo-data:/data/db
      - ./mongo-dump:/docker-entrypoint-initdb.d:ro
    environment:
      MONGO_INITDB_DATABASE: ${MONGO_DATABASE}

volumes:
  mongo-data:
